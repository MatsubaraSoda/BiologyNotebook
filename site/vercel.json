{
  "cleanUrls": true,
  "installCommand": "npm install",
  "buildCommand": "rm -rf .quartz && npx quartz plugin install && node ./scripts/patch-mermaid-version.mjs && npx quartz build -d ..",
  "outputDirectory": "public"
}
